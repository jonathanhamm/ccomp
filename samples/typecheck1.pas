program bob (input, output);
var a : integer; var c : real; var d : array[2332..3232] of real;
var torquemada : array[32..2332] of integer; var putin : array[333..32322] of real;
procedure lolwut (a : integer; b : real; c : real; kimjongil : array[14..1000] of real; bosstweed : array[1..1000] of real);
    begin
        if a <> b then
        call lolwut(a, b, c);
            call lolwut(a, c, b)
        ;
kimjongil[a*a+a] := 333*34;
b := c*b*a*c/3;
kimjongil := bosstweed;
call lolwut;
        c := kimjongil[a*b]
    end;


begin
a := 3;
    if (a*32 mod 4) <> c then
        call lolwut(234, 32, 23, 23, 23)
    else
        begin
            while a < c do
                begin
                    a := a*32*c;
                    c := a*23*23/4*32/23
                end
end;
        if (d[3] = putin[11+32+3]) and (torquemada[323] <> 432) or (a+c/c+32 <= putin[32]) then
            begin
                d[2] := 32
            end
        else
            begin
                putin[88*88*a] := 23.132+32/32+342+(3 mod 44)
            end
end.
